services:
  platform:
    environment:
      ENV: ${ENV}
      HTTP_PROTOCOL: ${HTTP_PROTOCOL}
      DOMAIN: ${DOMAIN}
      APP_NAME: ${APP_NAME}
      NEXT_PUBLIC_API_GRANT_TYPE: ${API_GRANT_TYPE}
      NEXT_PUBLIC_API_CLIENT_ID: ${API_CLIENT_ID}
      NEXT_PUBLIC_API_CLIENT_SECRET: ${API_CLIENT_SECRET}
      NEXT_PUBLIC_API_BASE_URL: ${HTTP_PROTOCOL}://api.${DOMAIN}

