services:
  platform:
    environment:
      ENV: ${ENV}
      HTTP_PROTOCOL: ${HTTP_PROTOCOL}
      DOMAIN: ${DOMAIN}
      APP_NAME: ${APP_NAME}
      NEXT_PUBLIC_API_BASE_URL: ${HTTP_PROTOCOL}://api.${DOMAIN}

